Drop your instrumental background track here as:

  background-music.mp3

The music toggle button (bottom-right, once the site loads) will play/pause
this file automatically. No code changes needed — just add the file with
this exact name. Keep the file reasonably small (under ~3-4MB) so the site
still loads quickly over mobile data.
